import { StyleSheet } from 'react-native';

export const gs = StyleSheet.create({
  fontBold: {
    fontWeight: 'bold',
  },
  flexRow: {
    flexDirection: 'row',
  },
  centerItems: {
    justifyContent: 'center',
    alignItems: 'center',
  },
});
